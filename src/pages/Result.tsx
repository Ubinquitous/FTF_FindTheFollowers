import React from 'react';
import Header from '../components/Header';

const Result = () => {
    return (
        <div>
            <Header />
            result
        </div>
    );
};

export default Result;